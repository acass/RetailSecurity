# OpenAI API Configuration
# Required for AI-powered chat features and natural language query processing
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Camera Configuration
# Camera source: 0 for default webcam, 1 for second camera, or RTSP URL for IP cameras
# Examples:
#   - Webcam: 0
#   - IP Camera: rtsp://username:password@192.168.1.100:554/stream
CAMERA_SOURCE=0

# Object Detection Configuration
# Confidence threshold for YOLO detections (0.0 to 1.0)
# Lower values = more detections but potentially more false positives
# Higher values = fewer detections but higher accuracy
CONFIDENCE_THRESHOLD=0.5

# YOLO Model Configuration
# Path to YOLO model file (relative to project root or absolute path)
# Available models: yolov8n.pt (nano), yolov8s.pt (small), yolov8m.pt (medium),
#                   yolov8l.pt (large), yolov8x.pt (extra large)
# Note: Larger models are more accurate but slower
MODEL_PATH=models/yolov8n.pt

# FastAPI Server Configuration
# Host and port for the REST API server
HOST=0.0.0.0
PORT=8000

# API Base URL (for Streamlit apps connecting to FastAPI backend)
# Use localhost when running locally, or your server IP/domain when deployed
API_BASE_URL=http://localhost:8000
